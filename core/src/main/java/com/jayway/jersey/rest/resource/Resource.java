package com.jayway.jersey.rest.resource;

/**
 * Base class for all resources.
 *
 * This class has the ability to reflectively generate an HTML
 * representation that lists the resources' reflection
 *
 */
public interface Resource {
/*
    protected <T> T context(Class<T> clazz) {
        ContextMap map = ResourceUtil.contextMap();
        return map.get(clazz);
    }

    protected <T> void addToContext(Class<T> clazz, Object instance) {
        ContextMap map = ResourceUtil.contextMap();
        map.put(clazz, instance);
    }
*/
}
